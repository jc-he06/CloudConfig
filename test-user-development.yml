feign:
  hystrix:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
        
        
spring:
  application:
    name: movie-store

server:
  port: 0

user:
  name: Chris-He
